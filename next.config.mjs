/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  // Bỏ turbo nếu bạn không cần:
  // experimental: {
  //   turbo: false, ❌ sai vì false không phải object
  // },
};

export default nextConfig;
